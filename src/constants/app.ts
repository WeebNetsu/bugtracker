const APP = {
    name: "Bug tracker",
    version: "0.0.01",
    /**
     * If app is in testing mode, some limitations are removed,
     * such as needing to solve captchas when logging in
     */
    testing: true,
};

export default APP;
